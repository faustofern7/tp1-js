let numerop = parseInt(prompt("Ingrese un número:"));

    if (numerop % 2 === 0) {
      document.write("El número " + numerop + " es divisible por 2.");
    } else {
      document.write("El número " + numerop + " no es divisible por 2.");
    }